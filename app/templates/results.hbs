<h1>Results</h1>
<div class="flex-container">
  {{#each model as |search|}}
  {{#if search.images.[0].url}}
      {{#link-to 'details' search.id}}<img src="{{search.images.[0].url}}" alt="Image" class="select">{{/link-to}}
  {{else}}
  {{/if}}
  {{/each}}
</div>
